// home_visit_tracking.dart
import 'package:flutter/material.dart';

class ReferLetterGeneratorPage extends StatelessWidget {
  const ReferLetterGeneratorPage({super.key});
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Home Visit Tracking')),
      body: const Center(child: Text('Home Visit Tracking Page')),
    );
  }
}
